const PageFooter = ({ children }) => {
  return (
    <div className="uk-card uk-card-secondary uk-card-body uk-light uk-overflow-hidden uk-margin">
      {children}
    </div>
  );
};

export default PageFooter;
