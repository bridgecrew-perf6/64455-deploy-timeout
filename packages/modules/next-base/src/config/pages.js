export const pageResolvers = new Map();

// Example - default (runs for all pages):
//
// pageResolvers.set('default', (client, page, options) => {});

// Example - specific, by page alias:
//
// pageResolvers.set('home', (client, page, options) => {});
